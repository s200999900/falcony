# Description: Word processing program using GTK+2.0
# URL: http://www.abisource.com
# Maintainer: Antonio SJ Musumeci, bile at landofbile dot com
# Depends on: aspell enchant fribidi wv

name=abiword
version=2.6.3
release=1
source=(http://www.abisource.com/downloads/abiword/$version/source/abiword-$version.tar.gz)

build ()
{
    cd $name-$version
    ./configure \
	--prefix=/usr \
        --with-libxml2 \
        --enable-extra-optimization \
	--with-aspell=yes \
        --enable-printing \
        --enable-threads \
        --enable-scripting
        make
    make DESTDIR=$PKG install
}
